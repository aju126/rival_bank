<%= form_for(@account_interest) do |f| %>
  <% if @account_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_interest.errors.count, "error") %> prohibited this account_interest from being saved:</h2>

      <ul>
      <% @account_interest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interest %><br>
    <%= f.text_field :interest %>
  </div>
  <div class="field">
    <%= f.label :account_name %><br>
    <%= f.text_field :account_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
